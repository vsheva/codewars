function basicOp(operation, value1, value2)
{
    switch(operation) {
        case "+":
            return value1 + value2;
            break;
        case "-":
            return value1 - value2;
            break;
            return
        case "*":
            return value1 * value2;
            break;
        case "/":
            return value1 / value2;
            break;
            default:
                "Введите оператор"
    }
    }

    console.log(basicOp('*', 4, 7))